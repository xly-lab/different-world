import { HtmlForMenu } from "interface/html";

const html: HtmlForMenu[] = [
  {
    title: "H5的新特性有哪些?",
    description:
      "画布(Canvas) API、地理(Geolocation) API、音频、视频API(audio,video)、localStorage和sessionStorage、webworker, websocket、新的一套标签 header,nav,footer,aside,article,section、web worker、HTML5 History两个新增的API、WebSocket",
    // value  小写
    value: "h5-new-feature",
  },
];

export default html;
